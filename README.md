# terraform-aws-conduiter-daemon
Terraform module to deploy a Conduiter daemon (EC2 + Docker) into your AWS account. Polls for transfer requests, encrypts and decrypts files end-to-end, and reads from / writes to S3.
